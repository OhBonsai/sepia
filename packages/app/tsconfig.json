{
  // app 横跨三个运行时，所以拆三份：main（node）、preload（node + DOM）、renderer（DOM）。
  // 这是「renderer 与 main 之间没有直接 import」（结构 4）的类型层地基——
  // 它们连全局类型都不共享。`bun run typecheck` 两份都跑。
  "files": [],
  "references": [{ "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.preload.json" },
    { "path": "./tsconfig.web.json" }]
}
