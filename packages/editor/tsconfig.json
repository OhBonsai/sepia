{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    // 跑在 renderer：有 DOM，没有 node（结构 3）。
    "types": []
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
