// @sepia/editor —— CM6 扩展、widget、markdown 结构判定（lezer 住在这里，不在 core）。
// 刻意不依赖 @sepia/ui（只共享变量名，T-20）与 @sepia/agent（能力上提到 app 装配）。
//
// Stage 2 起长出 extensions/ widgets/ structure/，见 001_boot.md §2.1。

export {}
