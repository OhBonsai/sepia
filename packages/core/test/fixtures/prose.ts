// 锚点标定集：**真实长文**（架构 §4.2 明写「用真实文章标定」），
// 逐字取自本仓库自己的两篇文档（`sepia-architecture.md` §4.4 与 `003_stage_playbook.md` §3）。
//
// 为什么是 `.ts` 而不是 `.md` + `readFileSync`：`core` 是叶子包，tsconfig 里**没有 node 类型**——
// 测试里一 import `node:fs` 就 TS2307。包边界连测试也一并管住了，这是好事：
// 它逼着标定集变成纯数据，而不是一次文件 IO。

/* eslint-disable unicorn/no-null -- 纯数据模块，无逻辑 */

export const PROSE_ARCHITECTURE = "### 4.4 渲染层\n\n**编辑器**：CodeMirror 6，源码态 live preview，元素级揭示。**MVP 覆盖全部 markdown 语法**——半套 live preview 比没有更糟：一部分渲染、一部分不渲染，纸面就成了既非源码也非成品的中间态。\n\n「渲染」按语法类别分四种含义，机制与代价差一个量级：\n\n| 类 | 语法 | 渲染的含义 | 机制 |\n|---|---|---|---|\n| **A 行内标记** | 强调、加粗、行内代码、删除线、链接、自动链接、行内公式、转义、硬换行 | 显示为效果，**光标进入该元素才露出标记** | **mark / inline-replace 二式** + 光标区间判定（行内公式是 replace + KaTeX widget，与表格同族，Stage 2 修订）。写完第一个，其余是**配置表** |\n| **B 块级排版** | 标题、引用（含嵌套）、无序/有序列表、任务列表、分隔线、frontmatter | 行首标记隐藏 + **视觉缩进对齐**，文本一个字节不动 | 行装饰 + padding。**最麻烦的一类**，见下 |\n| **C 块级 widget** | 表格、块级公式、textdiagram、图片 | 失焦渲染成图/表，光标进入变回源码 | 块 widget（L1）。**widget 内部的行内文本一律复用 A 类装饰管线，不自写第二套**（见下） |\n| **D 不渲染** | HTML 块与裸行内 HTML、缩进式代码块 | 按普通文本呈现，可加语法高亮 | — |\n\n**B 类为什么最麻烦**：列表与引用的缩进必须**视觉实现**而非改写文本——不能重新缩进字节（违反不变量 2），只能靠 padding 与 hanging indent 把 `- ` 藏掉并对齐。嵌套层级、有序列表的宽度变化（`9.` 与 `10.`）、软换行后的续行对齐，都要算。这是 CM6 live preview 公认最费工的部分。\n\n**D 类为什么排除**：任意 HTML 渲染带来安全与布局不可控（不变量里没有为它兜底的机制）；缩进式代码块与列表内缩进天然歧义，判错会毁掉排版。两者按源码呈现是更诚实的选择。\n\n**C 类 widget 内部的行内渲染归 A 类**（Stage 4 走查补的设计留白）：表格单元格这类 widget 内文本里的行内标记，**一律复用 A 类那条装饰管线，不许自写第二套**。自写的代价不是多几十行，是从此有两套会各自漂移的行内规则——走查看见的现象正是「网格画出来了，单元格里 `` `code` `` 还露着反引号」。两条边界：\n\n- **只做行内，不递归块级**（表格里不套表格、不套代码块）。实现上不靠自觉：让解析器自己执行这条规矩——把待渲染文本放回一个合成的表格行里解析，GFM 本就规定单元格内只有行内，于是 `# x` 在单元格里老老实实是三个字符，而不是一个标题。\n- **D 类的安全判断在 widget 内同样成立**：渲染器交出的是 DOM 节点而非 HTML 字符串，单元格里的 `<script>` 只会是六个字符。\n\n**流式**：解析层负责补全未闭合语法与块级 memo；揭示节奏为 24ms 批次、按词与标点边界 snap、无逐字补间；`prefers-reduced-motion` 命中则整块秒显。四条稳定性不变量：**冻结即定、结构块绝不闪 raw、揭示单调只增、节奏与 token 到达率解耦**。\n\n**CM6 接入的结构硬约束（Stage 2 实测得出，与下面的字节硬约束并列）**：\n① **块级 replace decoration 只能来自 StateField**——ViewPlugin 提供直接 `RangeError`，装饰引擎必须分两层（行内层随视口走 plugin、块级层全文档走 field）；\n② **跨换行的 replace 不许来自 plugin**（HardBreak 节点含换行符，隐藏范围要夹到行尾）；\n③ **⌘⇧V 在 Chromium 不派发 paste 事件**（它是编辑命令），粘贴逃生舱必须自己读剪贴板。\n\n**CM6 接入的字节硬约束（不变量 2，Stage 1 实测得出）**。CodeMirror 6 会**静默规范化换行**，这是不变量 2 最现实的一条威胁，且它在两层上都成立：\n\n- `EditorState.create()` 默认按 `DefaultSplit = /\\r\\n?|\\n/` 拆行——**CRLF 文件读进去就没有 CR 了**；\n- 即便显式设了 `EditorState.lineSeparator` facet，**`state.doc.toString()` 仍恒用 LF 拼行**：`Text.sliceString(from, to, lineSep = \"\\n\")` 的默认值是硬编码的，与 facet 无关，而 `toString()` 不传它。只有 `state.sliceDoc()` 才传 `state.lineBreak`。\n"

export const PROSE_PLAYBOOK = "## 3. `/harness` 看板\n\n> **裁决（2026-08-05，用户）：看板不做，钉死。** 110 / 130 / 140 连推三次之后，第三次被问的不再是「这次做不做」而是「要不要存在」——答案是不要。stage plan 的 frontmatter 与关闭记录里的遗留债表已经让进度与债务可见，不需要单独的渲染视图。本节及下面 §4 留作推理记录，不再指导实施；债务指标（豁免趋势、反向覆盖率、dispute、实测 vs 预算）继续由 frontmatter 记录、人在关闭时抽查，不做可视化。\n\n### 3.1 一条设计原则\n\n**不引入第二个真相源。** 任务状态的真相就是 `specs/plan/*.md` 本身；看板只读、只渲染。理由三条：AI 改状态时改 markdown 最自然（它本来就在改文件）；git 天然给你历史与责任；人可以直接编辑而不必学一个工具。\n\n### 3.2 数据来源\n\nstage plan 的 frontmatter + 正文里的勾选框：\n\n```yaml\n---\nstage: 2\ntitle: 语法\nstatus: in-progress        # todo | in-progress | blocked | done\ndod: 全语法 live preview；IME 不被打断；round-trip 通过\nchecks_added: 4\nchecks_reverse_verified: 3   # 反向验证做了几条\ndead_checks: 1               # 反向验证首轮空转数（口径见下）\nexemptions: 2\ndisputes: 1                  # > 0 表示有事等人裁决\nmeasured:\n  cold_start_ms: 780\n  bundle_mb: 142\n---\n```\n\n**`measured` 是每个 stage 自定义的开放字典，上面两个键只是示例。** 各 stage 只填自己真正测得、且有可比性的指标——例如冷启动是 Stage 1 才有的指标，Stage 0 的窗口里没有 page 可开，照抄这个键只会写下一个没有意义的数字、污染基线。债务面板按**键名**画趋势，某个 stage 缺这个键就不画那一段，不要补零。\n"
