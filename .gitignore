node_modules/
out/
dist/
.turbo/
.vite/
coverage/

# SEPIA_HARNESS_BYPASS 的留痕（002 §5.2）。只在本地产生，不进版本库。
.harness-bypass.log

.DS_Store
*.log

# Playwright smoke 的运行产物
test-results/

# 引擎产物（build-engine.ts 生成，真相在 vendor submodule 的锁定 tag）
packages/app/engine/

# 本机工具的临时目录（agent 会话续跑状态、随手截图），与项目产物无关
.omo/
.rv6/
.rva/
