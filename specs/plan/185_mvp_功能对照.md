# 185 · MVP 功能对照（实现 vs 完整设计）

> 2026-08-06，Stage 7 计划口径（180 走完后的状态）对照三份设计真相：
> `sepia-prototype-features.md`（完整版盘点 H/F/S/G）、`sepia-mvp-happy-path.md`（v1 真理源）、
> `sepia-mvp-non-goals.md`（反清单）。
> 状态：✅ 已有/Stage 7 将有 ｜ 🟡 有但形态与设计有距 ｜ ⛔ 设计裁定 v1 不做（符合预期）｜
> ❌ **v1 该有但没排期（缺口）** ｜ ❓ 需查证/需裁

## 〇 总览

| 类 | 计数 |
|---|---|
| ✅ 实现且对齐 | 22 |
| 🟡 实现但形态有距 | 8 |
| ⛔ 设计裁定不进 v1（衰减/非目标，符合预期） | 12 |
| ❌ **v1 真理源点名但从未排期** | **4** |
| ❓ 需裁决 | 2 |

## 一 · 主页（H）

| # | 功能 | 状态 | 说明 |
|---|---|---|---|
| H1 | Workspace 多 book | ⛔→🟡 | D-16 锁单 book；衰减形态"左栏退化为 book 名展示"——现主页无左栏，book 名未展示（视觉差距清单 ①） |
| H2 | 新建 Workspace | ⛔ | 多 book v2 |
| H3 | book 内搜索框 | ⛔ | non-goals 明拒；`@` 文件名搜索顶替（已有） |
| H4 | 最近的笔记 | 🟡 | 6b 已有列表直达；相对时间分组（12 分钟前→昨天→上周）未做（视觉②） |
| H5 | 新建笔记入口 | 🟡 | `files.new` 命令在；主页无按钮（入口见 ⌘/ 裁决 ❓B） |
| H6 | 设置入口/⌘, | ⛔ | 架构 §1.3：v1 无设置 UI，改 config.json |
| H7 | 帮助 | ⛔ | 原型即占位 |

## 二 · Page 页（F）

| # | 功能 | 状态 | 说明 |
|---|---|---|---|
| F1 | Living 编辑（元素级揭示） | ✅ | Stage 2，且超出衰减承诺（全语法而非"标题粗斜体代码块"） |
| **F2** | **标准快捷键集 D-26**（⌘B/⌘I/⌘1~6/⌘⌥C/列表续行/Tab 缩进…） | ❌ | **分镜 1 点名，任何 stage 都没排**。缺口 #1 |
| F3 | 自动保存 + 静默 commit | ✅ | 5a |
| **F4** | **`/` 组件菜单**（textdiagram/image 两项） | ❌ | **分镜 3 点名，从未排期**。缺口 #2 前半 |
| F5 | textdiagram 失焦渲染 | 🟡 | widget 机制在（Stage 2），但渲染是**等宽 pre 盒占位**，不是真 mermaid 图。缺口 #2 后半 |
| F6 | shader 块 | ⛔ | D-27 移出 MVP，glsl 按普通代码块 ✅ |
| F7 | 图片 ⌘V/拖拽落盘 | 🟡 | 6b 已有；落 `img/` 固定值，设计是 `assets/` 且 D-40 可配置（小偏差，记档即可） |
| F8 | Meta info 属性表 | ⛔ | 不在分镜（真理源判定不进 v1）；frontmatter 有 B 类基础装饰 |
| F9 | markup 对话（⌘K 三阶段） | ✅ | Stage 4，含动词列/家具/缺席态 |
| F10 | 同线程追问/换模型重试 | ✅ | Stage 4 |
| F11 | 徽章（diff 取自 git） | ✅ | 5b，成对 commit + api.git.diff |
| F12 | ⌘⇧H 还白 | ✅ | 5b |
| F13 | 对话列表面板（含孤儿区） | 🟡 | 5b 已有；原型是"右侧区复用体系"（面板/双屏/浏览器三者互斥同一区），现为独立面板（视觉③） |
| F14 | 多文件 Tab | 🟡 | 6b 已有；缺 tab 栏最左 ⌂ 按钮（现为"全关回主页"，视觉④） |
| F15 | `@` 引用插入 | ✅ | 6b，标准 md 链接 |
| F16 | @ 双屏（⌘点击右栏编辑器） | ⛔ | **核对分镜：F16 不在任何分镜里**（分镜 9 只用到 C4）——按真理源本就不进 v1。6b 的降级（新 tab）无损 MVP，**双屏债可关闭** |
| F17 | 连接面板 | ⛔ | 不在分镜；反向链接与 H3 同前提（待定项 3） |
| F18 | 外链内嵌浏览器 | ⛔→❓ | 衰减裁定"交系统浏览器打开"——**该衰减形态本身是否落实过未验证**（正文外链点击行为），归入验收走查确认 |
| **F19** | **Agent 状态点**（启动中灰→就绪实心） | ❌ | 缺席线/⌘K 文案有（Stage 3）；**角落状态点没有，而分镜 0 上镜**（"一枚小灰点，3 秒后实心"）。缺口 #3 |
| F20 | MCP/SKILL 浮层 | ⛔ | 衰减：退化为就绪状态点（并入 F19） |
| F21 | ⌘⇧I 信息浮层 | ✅ | Stage 7 |
| F22 | 保存微反馈（纸角一闪） | ✅ | Stage 7（失败警示点 5a 已有，成对设计） |
| F23 | ⌘/ 快捷键看板 | ❓ | **设计冲突见裁决 B** |

## 三 · 设置页（S1–S5）

⛔ 整页不进 v1（架构 §1.3；改 config.json 即设置）。

## 四 · 全局（G）

| # | 状态 | 说明 |
|---|---|---|
| G1 纸永远可写 | ✅ | 不变量 1，kill -9 实证 |
| G2 文件即真相 | ✅ | 不变量 2，round-trip 两期 + CRLF 战史 |
| G3 book=git | ✅ | 5a/5b |
| G4 存储分层 | ✅ | 按 T-25 改判 ~/.sepia（G4 原文是改判前旧文） |
| G5 键盘优先 | 🟡 | 主链可键盘；⌘K 浮层内动词列的上下键选中未验证（归验收走查） |

## 五 · happy-path 分镜逐镜核（真理源）

| 分镜 | 状态 | 缺什么 |
|---|---|---|
| 0/12 秒开 | 🟡 | 秒开 ✅；**Agent 状态点缺**（缺口 #3） |
| 1 书写 | 🟡 | 揭示/IME ✅；**⌘B 等标准快捷键缺**（缺口 #1） |
| 2 贴图 | ✅ | （目录名偏差记档） |
| 3 `/` 菜单+mermaid | ❌ | **菜单没有；mermaid 是 pre 盒**（缺口 #2） |
| 5–8 markup 全链 | ✅ | Aha #2 完整 |
| 9 `@` 引用 | ❓ | 插入 ✅；**"对话可带该文做 context"（C4）断链疑点**：组装器认 @content 块（Stage 4），6b 插入的是 md 链接——**没有任何一环把正文里的引用喂给组装器**。缺口 #4，需查证 |
| 10 还白 | ✅ | |
| 11 ⌘Q 即关 | ✅ | Stage 7 拦截仅限写盘失败时 |
| 13 线程回看 | ✅ | |
| 14 发文零参与 | ✅ | 纯 md 红利 |
| 支线 X 缺席 | ✅ | |

## 六 · 视觉差距清单（骨架级，视觉专项 B 组的输入）

① 主页无左栏（book 名 + 设置/帮助占位的那条 220px）｜② 最近列表无相对时间分组｜
③ 右侧区未成体系（面板应与未来双屏/浏览器共用一个可关右栏）｜④ tab 栏无 ⌂｜
⑤ 纸顶三图标（属性/连接/对话）整排未做（属性⛔连接⛔、对话面板入口现走徽章点击）｜
⑥ 徽章/横条/浮层的间距字号微调（原有 B 组债）

## 七 · 需裁决的两项

**A · 四个缺口进不进 Stage 7**（分镜点名 = 真理源要求，估工作量）：
- #1 F2 标准快捷键集：中（CM6 keymap toggle 一组 + 列表续行/Tab 缩进）
- #2 F4+F5 `/` 菜单 + mermaid 真渲染：中偏大（菜单小；mermaid 要引依赖，惰性 chunk，纪律 12 复测）
- #3 F19 状态点：小（状态已有，补一个点的 UI）
- #4 C4 @content 喂给组装器：查证后小～中（解析正文 @ 链接 → @content 块）

**B · ⌘/ 形态冲突（我在 180 里埋的雷，自首）**：180 计划把 ⌘/ 写成**可执行命令面板**；
但设计 D-32/F23 定的是**只读快捷键看板**（分组键帽、搜索过滤、上下文置灰），且 non-goals
表 2 明确拒绝命令面板。**建议按 D-32 改 180**；由此 threads.panel/files 的入口债不能靠
面板解决，改为：threads.panel 绑键（如 ⌘⇧L）+ 主页新建按钮（H5 顺手），files 其余两条留树右键（已有）。
