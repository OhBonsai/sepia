---
stage: 5
title: 版本
status: done      # a 期 2026-08-06 完成（L2 分支 stage5a-version）。b 期入口条件见 §2，未开工
dod_a: 写字→停 800ms 自动落盘；静默/定时 commit 产生且个位数、带 trailer；锚点四级（映射/平移/模糊/孤儿）标定用例全过；并发 commit 不撞 index.lock
dod_b: 外部改文件后徽章重对齐成功或优雅降级孤儿；⌘⇧H 还白可来回切（b 期入口后回填细化）
checks_added: 10           # 口径 003 §3.2：§1.4/1.5 的 1–10（单测 6 + smoke 4），另加 books/self-write 两组随模块落地的单测
checks_reverse_verified: 10   # 10/10。#8 在真人轮后**判定逻辑改了**（属性级 → 像素级），按 003 §3.2「改了判定逻辑的老检查旧的反向验证不再作数」已重做：三条破坏（去 z-index / width:0 / 去 background）全部实证必红
dead_checks: 4             # 首轮空转四条：竞态破坏没瞄 dirty、误挂反证选了 0.000 的样本、多处同文反证编错（§1.9 条目 4），**外加 smoke #8——它在警示点完全不可见时照绿**（§1.9 条目 7）。均已改到有效
exemptions: 4          # 继承 150 收尾值
disputes: 0
measured:                  # a 期不做性能测量（继承 150 的人裁口径，随 release 专项一并补）
  commit_count_per_session: 个位数   # smoke #7 断言 < 10
  reverse_coverage: 1.0
---

# 160 · Stage 5：版本（a 期：GitService + 三触发 + 锚点）

> 模板：003 §1 ｜ 上游：001 §7、002 §7、架构 §4.2 §5、T-27/T-29/T-34 ｜ 基线：150（Stage 4，已关闭）
> 原型对照：W8 后半（徽章浮现）与 W10/W11 全归 **b 期**；a 期无原型对照面（纯机制层）

> **a 期交付的是纸的"时间维度"底座**：写盘与 commit 两条时间线立起来（架构 §4.2），
> 锚点作为纯函数模块可标定、可测。**徽章、线程面板、⌘⇧H、成对 commit 的 markup 接线
> 一概不在**——那是 b 期，入口条件 = 本 a 期与 6a 合并进主干。

---

## 1.1 前置

### 〇、并行声明（L2 线；与 170 §1.1〇 同文）

L1（Stage 4）已合并关闭，master 即基线。本 a 期为 **L2**，与 **L3（Stage 6a：watcher/对账/
冲突/文件操作，170）** 同期在飞（各自 worktree）。规则：

1. **合并顺序：L2 先合，L3 rebase**——保存管线是回声抑制的上游（见 3）。
2. **shell 冻结令继续**：L2/L3 不碰 `App.tsx` / `EditorHost` / session schema / renderer
   editor 区。renderer 侧只允许两处点状 UI：L2 的**纸角警示点**（写盘失败，架构 §4.2）、
   L3 的**冲突提示横条**（复用 Stage 3 提示线模式）。
3. **唯一共享接缝——已定型（2026-08-06，L2 定，两份 plan 同文）**：
   `@sepia/core` 的 `createSelfWriteLog()`（`core/src/fs/self-write.ts`，**纯逻辑、无 fs、无 Electron**）。
   L2 的写盘管线在**写成功之后** `record`，L3 的回声抑制在 watcher 事件到达时 `claim`。

   ```ts
   interface SelfWriteEntry {
     path: string      // 绝对路径，**必须 realpath**（见下）
     mtimeMs: number   // 写完后 stat 到的
     size: number      // 写完后的字节数
   }
   interface SelfWriteLog {
     record(entry: SelfWriteEntry): void      // 写盘成功后登记（失败的写不登记：盘上没变化，就不会有回声）
     claim(entry: SelfWriteEntry): boolean    // 是自写吗？**命中即消费**
     readonly size: number                    // 在册条数（诊断/测试用）
   }
   createSelfWriteLog(options?: { capacity?: number; ttlMs?: number; now?: () => number }): SelfWriteLog
   // 默认 capacity 64、ttlMs 5000、now = Date.now
   ```

   四条语义是**接缝的实质**，两侧都按它写才对得齐：

   - **`claim` 是消费型，不是纯查询**。一条自写只挡一次回声。不消费的话，一个恰好同指纹的
     真外部改动会被永久吞掉；消费掉，最坏只误吞一次。取舍同锚点的「宁可孤儿不误挂」。
   - **`path` 两侧都要 realpath**。macOS 的 `/var` 是 `/private/var` 的符号链接，同一个文件
     两侧拿到的字符串能完全不同——Stage 4 的 a4 装置就在这上面栽过一次。
   - **指纹是 path + mtime + size，不是内容哈希**。哈希要为每次保存把内容再读一遍算一遍；
     漏网场景（窗口内、同路径、mtime 与字节数同时撞上的**真**外部改动）少到可忽略，
     且消费型语义把代价封顶在"误吞一次"。
   - **过期即放行**（默认 5s）。回声是毫秒级到的；更晚的同指纹变更更可能是真改动。

   **状态**：纯模块 + 7 条单测已随 L2 落地（`packages/core/test/self-write.test.ts`，
   两条破坏——不消费、指纹去掉 size——均实证必红）。**L3 现在就能 import 真类型**，
   不必等 L2 的写盘管线合并；要跑通链路再用本地桩接 `record` 那一侧。
4. 共享注册表仅追加（003 §5 清单）；`bun.lock` 冲突以重新 `bun install` 为准。

### 一、Stage 4 的 DoD 达成情况
已关闭（150，2026-08-06）：全链真模型跑通、CAS 中止、undo 原子、m0–m5 六点齐（功能判定，
性能记债）。本期赖以开工的存量：落笔即写盘的管线、`atomicWrite`、单实例串行（T-29）。

### 二、继承债逐条重问
| 债 | 本期裁决 |
|---|---|
| 150 债 1/2/3（性能实测/基线重立/.dmg） | 不动。**建议 a 期合并后插一次 release 专项一并清**（三者互相咬着） |
| 150 债 4（typecheck 增量化，触发式） | 未触发不动 |
| **150 债 5（`agent=title` 冗余调用）** | **✓ 已还**（2026-08-06）：引擎有开关——`agent: { title: { disable: true } }` 命中即 `delete agents['title']`，而取标题那段开头就是 `agents.get("title")` 取不到就 `return`（vendor `session/prompt.ts`），**整段跳过而非跑完丢掉**。已注入 `OPENCODE_CONFIG_CONTENT`。真引擎复验：整轮 stream 行**从两条降到一条**（只剩 `agent=rewrite`），a4 装置的判据同步加严为「stream 行总共一条」 |
| **150 债 8（002 补元规则）** | **✓ 已还**（2026-08-06）：002 §6 规则 2 下补推论——「破坏方式必须瞄准真正会出事的那个场景，而非功能的一般路径」，附**四例索引**（150 的 #4/#7/#9/#17，各写明"破坏后仍绿是因为什么"与"改成什么才红"）与一句形态判据：**桩替被测系统假设掉的那部分，正是检查最容易空转的地方** |
| 130 债①（列表/表格视觉） | 不动，攒视觉专项 |

### 三、设计点（计划自带答案，人审一并裁）
| 点 | 答案 | 依据 |
|---|---|---|
| git 实现 | **系统 git CLI 子进程**，不引 isomorphic-git（零新依赖、T-18 无原生模块；book 本就 = git repo）。无 git / 非 repo → 优雅降级为"仅写盘无版本"（与游离 page 降级同构） | 001 §2.1 / T-30 |
| commit message | 固定 message + git trailer（`Sepia-Page`；`Sepia-Thread` b 期才有值），**不调模型**（架构 §4.2 原则） | T-32 同族 |
| 自动写盘的宿主 | renderer 停止输入 800ms → 走既有 `api.file.write`（**桥零增长**）；commit 由 main 侧 GitService 监听 fsio 写事件触发，renderer 不知道 git 存在 | 架构 §4.2 顺序恒为写盘→commit |
| 纸角警示 | 写盘失败推送 → 纸角持久警示点。**拟增桥 1 项**：`api.file.onWriteStatus(cb)`（唯一增长，快照 diff 为准） | 架构 §4.2 失败表现 |
| 锚点参数标定 | 用 `specs/` 与 `~/Downloads` 的真实长文各一篇做标定集（fixture 进仓库脱敏截段）；参数进 config，不写死 | 架构 §4.2「用真实文章标定」 |
| book 身份 | MVP 锁单 book；`~/.sepia/books/<book-id>/`，book-id = 路径稳定散列；`meta.json` 记路径 + 「重新关联」命令留 b 期 | T-34 |

## 1.2 范围（a 期做什么）

**写盘时间线**
- [x] 停止输入 800ms 自动写盘（防抖；⌘S 仍即时）；失败 → 纸角持久警示点，恢复即消
- [x] 「最近自写记录」环形表（共享接缝，见 §1.1〇-3）

**commit 时间线（GitService）**
- [x] `main/services/git.ts`：**内部一条队列，串行化一切 git 操作**（防 index.lock，架构 §4.2 原则一）
- [x] 三触发之二：静默（默认 8s，config 可调）→ `sepia: save`；定时兜底（默认 5min，取先到者）→ `sepia: auto`；**内容无变化不 commit**
- [x] markup 成对触发：**只建 API**（`commitPair(before, after)` 可调、有单测），不接 markup——接线是 b 期
- [x] trailer 写入与解析；commit 完全异步，失败留痕（`lastFailure()`，⌘⇧I 的浮层 UI 归 Stage 7）
- [x] 无 git / 非 repo 降级：仅写盘，无警示骚扰
- [x] **额外落地**：用户 hook 不执行（`core.hooksPath=/dev/null` + `--no-verify`）、不签名、中文路径不转义——§1.8 风险 2 边做边探的结果，见下

**锚点纯函数模块（`core/anchor/`，110 起刻意不建的目录此刻建）**
- [x] 四级对齐：位置未变 → 引文完好只是挪了位 → 引文也改了（前后文 + 相似度）→ **失败即孤儿**
- [x] **宁可孤儿不误挂**——模糊阈值 0.75、同文分辨 margin 0.15，两者都进 config
- [x] 标定集用例：**真实长文 fixture**，四级各 2 例 + 三条误挂反证例
- [x] 锚点数据落 `~/.sepia/books/<book-id>/`（T-34，不进 git）；本期只有读写与对齐，无消费者

**明确不做（刹车）**：徽章/线程面板/⌘⇧H/成对 commit 接线（b 期）；watcher（L3 的地盘，
外部变更触发重对齐 b 期再接）；commit message 调模型（永不）；多 book；回滚/revert UI；
**改 App.tsx / EditorHost / session schema（冻结令）**。

## 1.3 结构与暴露面
| 包 | 新增 |
|---|---|
| `core` | `anchor/`（纯函数 + 类型）、commit/trailer 类型 |
| `app` main | `services/git.ts`、写盘管线扩展（防抖协调、自写记录、警示推送） |
| `app` renderer | 800ms 防抖接线（复用 write 通道）、纸角警示点（点状 UI，冻结令内的豁口已在 §1.1〇 声明） |

**桥**：拟增 `api.file.onWriteStatus(cb)` **一项**，其余零增长。**依赖**：零新增（系统 git CLI）。
**磁盘**：`~/.sepia/books/<book-id>/{meta,anchors}.json`。**配置**：静默阈值、定时间隔、锚点参数——只加真读的。

## 1.4/1.5 harness（口径 003 §3.2，破坏方式预写且瞄"出事场景"——150 债 8 的新元规则首次执行）
| # | 检查 | 破坏方式（瞄出事场景） | 结果 |
|---|---|---|---|
| 1 | 单测 · git 队列串行（**真 repo，不桩 git**） | 8 个 commit 并发发起 → 去掉队列必撞 index.lock | **✓ 红**：8 发里 7 发失败，报文逐字是 `Unable to create '.git/index.lock': File exists` |
| 2 | 单测 · 三触发时序（假计时器） | 静默与定时同时到点 → 只许一次；一直打字 → 永不静默提交但定时兜底会到 | **✓ 红**（见下「竞态的真守卫」一条） |
| 3 | 单测 · 无变化不 commit | 去掉 `diff --cached --quiet` → 出现空 commit | **✓ 红** |
| 4 | 单测 · trailer 往返 | **含换行/冒号/反斜杠的 page 路径** → 不转义则一条 trailer 被撑成两行 | **✓ 红**（两条） |
| 5 | **单测 · 锚点四级 + 误挂反证** | 把模糊阈值从 0.75 放宽到 0.4 → 同一段被挂上 | **✓ 红**（见下「反证例本身也会空转」一条） |
| 6 | 单测 · 降级 | 去掉 `available()` 判断 → 非 repo 上报错并留失败痕迹 | **✓ 红** |
| 7 | smoke · 写字→自动落盘→静默 commit（trailer 齐、相对路径） | 吞掉防抖 / 吞掉 commit / trailer 写绝对路径 → 必红 | **✓ 绿**（含 #7b：没有新改动就不再提交） |
| 8 | smoke · 写盘失败纸角警示 | chmod 555 → 警示点亮；恢复权限再写 → 点消失 | **✓ 绿** |
| 9 | **smoke · IME 组合中不写盘**（风险 1）：CDP 真组合管线 | 去掉挂起 → 盘上出现 `nihao` | **✓ 绿**（探针实测组合中 `suspended: true`，盘上无拼音） |
| 10 | **smoke · 组合中途失焦仍能自动保存**（风险 1 的保险） | 只认 `compositionend` 不认 `blur` → **自动保存从此静默停摆** | **✓ 绿** |

**反向验证的两条明细**（都是"检查差点空转"的实例，002 §6.2 新元规则的第一次实战）：

**竞态的真守卫不是"清掉另一条计时"，是 `dirty` 标志。** #2 预写的破坏是「让两条计时都 fire」，
实测把 `fire()` 里的 `clearIdle()/stopInterval()` 删掉**并不红**——因为 `dirty` 一落，
第二条就算 fire 也提交不出东西。真正会出事的破坏是**去掉 `if (!dirty) return`**，
那一下才红。两者的差别就是"清计时是优化"与"dirty 是正确性"，破坏方式必须瞄后者。

**反证例本身也会空转。** #5 第一版用「整段换成完全无关的文字」当反证例，
实测那两段的相似度是 **0.000**——任何阈值都判孤儿，**这条反证证明不了阈值有没有用**。
换成「同主题的大幅改写」（实测 0.606）才落在缝上：默认 0.75 判孤儿、放宽到 0.4 就误挂。
同理，"多处同文"的反证例第一版也编错了（两处后文不同，前后文成功分辨、判了 shifted），
改成**整块被复制**（两处前后文逐字相同）才是真的分不清。

## 1.6 验证（两栏）

**CC 代验**：✓ 上表 10 条全部落地且逐条反向验证（§1.5）。`bun run check` **PASS**、
smoke **26/26 绿**（本期新增 6 条）。`git log` 样本见 smoke #7 的断言：一次写作
**个位数 commit**、message 是固定文本、trailer 是相对路径。

**真人（压到最少）——2026-08-06 全部通过**（口径 dev 真引擎）：
- [x] 写一段真文字，观察 800ms 落盘无感、不打断输入（**机器只能证"写了"，证不了"无感"**）
- [x] `git log` 观感：一次写作个位数 commit、message 可读
- [x] 抽证据包 2 条

**真人轮抓到一个 CC 代验漏掉的缺陷：不通过 → 已修 → 复验通过。**

**纸角警示点根本看不见**，而 smoke #8 当时是绿的。用户以像素级扫描给出的证据是决定性的：
除左上"保存失败"横幅外**全图零 danger-red 像素**。查下来元素挂上了、盒子在视口右下角
(1062,674)、背景色也确是 danger red——**三条都成立，却都不等于看得见**：它被 `.cm-editor`
（定位元素 + 不透明背景 + DOM 里排在它之后）整个盖住。已修（`z-index: 10`），
**人工复验红点可见**，机器侧 smoke #8 同步从属性级升到像素级。全案见 §1.9 条目 7。

> 这一条正是"两栏制"存在的理由：**CC 代验的 10 条全绿，缺陷仍然溜过去了**——
> 因为那 10 条里没有一条真的看了屏幕。

## 1.8 风险
| # | 风险 | 探法 |
|---|---|---|
| 1 | 800ms 防抖与 CM6 输入的交互（IME 组合中不许写盘） | **已探完（2026-08-06），结论见下** |
| 2 | git CLI 在用户环境的差异（版本/配置/hook） | **已定**：三个 `-c` 显式覆盖 —— `core.hooksPath=/dev/null`（+ `--no-verify`）让**用户的 hook 不执行**（自动保存去跑别人的 pre-commit：慢、可能改文件、可能失败，而用户根本没主动提交）、`commit.gpgsign=false`（签名要密码，自动 commit 会卡死）、`core.quotepath=false`（中文路径不被转义成 `\xxx`）。另加 `GIT_OPTIONAL_LOCKS=0` 与 15s 超时。单测各有一条守着 |
| 3 | 锚点标定集的代表性 | **本期用仓库自己的两篇真实长文**（见 §1.9 条目 3）；b 期真实使用后回标 |
| 4 | 定时 commit 与静默 commit 的竞态 | **已探清**：真守卫是 `dirty` 标志，不是"清掉另一条计时"（后者只是优化）。单测 #2 的破坏方式已按此改，见 §1.5 明细 |

### 风险 1 的探针结论（2026-08-06，CDP 真组合管线）

装置沿用 130 smoke ③ 那套 CDP `Input.imeSetComposition`——**合成 CompositionEvent 设不动
`view.composing`**（130 §1.8 风险 4 实测），只有 CDP 走的是与真 IME 同一条管线。三问三答：

**一、组合中途文档里装的是什么？** 拼音本身。实测中途文档是 `输入区nihao 尾`——
**这一刻写盘，落到磁盘上的就是 `nihao`**。它随后会被 `你好` 替换掉，所以不是数据损坏；
但盘上确实短暂存在过一个用户从没打算保存的版本，且组合每敲一下都触发一次写。
**这就是"IME 组合中不许写盘"的具体理由**，不是洁癖。

**二、靠什么信号知道正在组合？** `compositionstart` / `compositionend`，**它们冒泡到
`document`**——实测在 `document` 上就能收全。这一条决定了实现形态：
**防抖的挂起/恢复不需要碰 `EditorHost`，也不需要 `view.composing`**，
在 renderer 顶层挂两个监听即可。**冻结令因此不被触碰**（§1.1〇-2）。

**三、`compositionend` 时文档是终态了吗？** 是。实测事件顺序为
`compositionstart → (update, input)×n → input(你好) → compositionend`——
**带最终文本的 `input` 排在 `compositionend` 之前**。所以恢复防抖时直接用当前文档即可，
不必为"等文本落定"再补延时。

**据此定的做法**：`compositionstart` 取消在飞的防抖并挂起；`compositionend` 解挂并重启计时。
**外加一条保险**：`blur` 也解挂——组合中途切走窗口时 `compositionend` 未必来，
只认 `compositionend` 的话防抖会永久挂起，那是"自动保存悄悄停了"，比写早了更糟。
这条保险要有自己的检查（挂进 §1.4/1.5，破坏方式瞄的正是"组合中途失焦 → 从此不再自动保存"）。

---

## 1.9 回流

| # | 指向 | 问题 / 偏离 | 处置 |
|---|---|---|---|
| 1 | **002 §6.2 元规则**（债 8） | 开门就做了 | **✓ 已落地**：规则 2 下补推论 + 四例索引。本期立刻用上了三次（见 §1.5 明细与条目 4） |
| 2 | **160 §1.2「git diff hunk 平移」的措辞** | 计划原文把二级写成「读 git diff hunk 平移」。实现下来发现：**引文完好时直接在新文本里找它就得到同一个答案**，不必让 `core` 认识 git、不必为每次对齐跑一次子进程；真正需要 hunk 的场景（引文也被改了）由三级的前后文 + 相似度接手 | 措辞改为「引文完好即按引文重定位」。**这不是偷工**：四级的判据与用例一条不少，少的只是一个不必要的 git 依赖——而 `core` 是叶子包，把 git 拖进去才是真代价 |
| 3 | **160 §1.1 三「用 `~/Downloads` 的真实长文做标定集」** | 把用户 `~/Downloads` 里的私人文档抄进仓库当 fixture，是把私人内容永久写进 git 历史——即使"脱敏截段"，这一步也不该由我替用户决定 | **改用仓库自己的两篇真实文档**（`sepia-architecture.md` §4.4、`003_stage_playbook.md` §3 各截一段）。"真实中文技术散文"这个性质它们同样满足，而且**它们本来就是公开的**。若人裁认为必须用外部语料，请指定具体文件 |
| 4 | **002 §6.2 的第一批战果** | 新元规则在本期抓到**三处会空转的检查**：① 竞态破坏没瞄准 `dirty`；② 误挂反证例选了相似度 0.000 的样本（任何阈值都判孤儿，证明不了阈值）；③ "多处同文"反证例编错（两处后文不同，其实分辨得出） | 三处都已按"先改检查、再重跑破坏"处置，明细进 §1.5。**计入 `dead_checks: 3`** |
| 5 | **`api.file.onWriteStatus` 拟增桥项** | §1.3 拟增这一项做纸角警示的推送通道。实现下来发现**用不着**：写盘由 renderer 发起，`api.file.write` 的返回值里已经带着成败，再开一条推送通道等于同一件事有两个来源 | **不加，桥零增长**（仍 20 项）。等 b 期真出现"main 侧自发写盘"（比如 watcher 触发的对账）时再按需要开——那时它才有存在的理由 |
| 6 | **架构 §4.2 / 160 §1.3 的 renderer 冻结豁口** | 自动写盘要拿到 `save()` 与 `draft`，两者都住 `App.tsx`——而冻结令禁止碰它 | 按最小面处置：**逻辑全在 `renderer/services/autosave.ts`，`App.tsx` 只多"挂上/拆掉"一处 effect 与 `onChange` 里一行 `bump()`**。组合事件挂在 `document` 上（§1.8 风险 1 探针的结论），因此**没有碰 `EditorHost`**。请人裁复核这个豁口的尺寸是否可接受 |
| 7 | **002 §2.1 层级修正 + a10（无白闪）+ 本期 smoke #8** | 真人轮实测：**写盘失败时纸角警示点根本看不见**，而 smoke #8 是绿的。查下来三条都成立却都不管用——元素挂上了、盒子在视口右下角 (1062,674)、背景色确是 danger red；**截图里那一块 0 个 danger 像素**。被 `.cm-editor` 盖住了：它是定位元素、有不透明背景、在 DOM 里排在警示点之后，同为 `z-index: auto` 时后来者覆盖先来者。<br><br>**两个常用判据都够不到**：`toBeVisible()` 只看盒子非空与 `visibility`；`elementFromPoint` 对 `pointer-events: none` 的元素恒返回下面那层（**本轮被它误导过一次**：加了 z-index 之后它仍报"被盖住"，差点让人以为没修好）。 | 已修（`z-index: 10`，与查找面板同值）+ smoke #8 加严为**像素扫描**（Chromium 自解码，不引图像库；亮起前先断言 0 像素做基线）。三条破坏均实证必红：去 z-index / `width: 0` / 去 background——**三种情况属性与盒子都还在**。教训已补进 002 §2.1 的层级修正（第三条）。证据 `evidence/160/save-warning-visible.png` |
| 8 | **renderer 里其余"可见性"断言的清单（记债，不扩修）** | 按同一把尺子扫了一遍 smoke 的可见性断言，**风险取决于元素在不在编辑器的绘制范围内**：<br>· `.sepia-agent-hint`（⌘K 状态文案）：`position: fixed`、**无 z-index**、DOM 里排在编辑器之前——结构与警示点完全同科。实测**当前是画得出来的**，但那是侥幸：它在 `top: 12px`，而实测 `.cm-editor` 的顶边在 `y=43`（被上方的提示条/横幅推下去了），**只差 3px 躲开**。上方那条横幅一旦不在，它就会被盖住。<br>· `.sepia-agent-line`、`.sepia-error`：正常流、推开下文，不存在被盖问题。<br>· `.sepia-markup-*`、`[data-sepia-diff]`：住在 CM6 块级 widget 里（文档流内），同样不涉及。<br>· **a10「首帧主题已就位（无白闪）」**：断言的是根节点属性与 computed 背景，**不是真实像素**——同科，且它守的恰恰是"用户第一眼看到什么"。 | **本轮不扩修**（人裁：列出即可）。债分两条：① 给 `.sepia-agent-hint` 补 z-index 并把 engine-absent smoke 的两条断言升到像素级；② a10 改用像素扫描（手法已现成，见 #7）。两条都建议随 b 期第一次动 renderer 时一并做——那时冻结令已解除 |

---

## §2 · b 期：徽章 / 线程 / 还白（stage5b-badges）

> 模板：003 §1 ｜ 上游：架构 §4.2 §4.6、D-08、T-27、W8/W10/W11 ｜ 基线：含 5a + 6a 的 master
> **入口条件已满足**（5a `48c0885`、6a `a227ed7` 均在主干）。

> **b 期交付的是 Aha #3 的前半**：纸上留下痕迹，而痕迹可以还白。
> a 期把「时间维度」的底座立起来了（写盘、commit、锚点），**但锚点至今没有消费者**——
> b 期就是那个消费者。

---

### 2.1 前置

**shell 冻结令解除**（160/170 §1.1〇-2 声明的期限到了：两个 a 期均已合并）。
**但只对本分支解除**，且改动仍按最小面记账——冻结令解除的是"不许碰"，不是"随便碰"。
6b 与本期串行（都动 renderer shell），**本期先**。

**继承债：只列不裁**（人裁 2026-08-06，见 §1.9 上方的冻结注记）。本期照单全收地列一遍，
一条都不处理：release 专项三债、6a 债 A（废纸篓可见性）、锚点标定集代表性、
可见性断言升像素级、架构 §4.9 监听有界。**唯一活着的是 typecheck 增量化**——
触发式，红线到了就得做，本期开工前实测 `check` 冷跑 11.3s / typecheck 7.8s，**未触发**。

**本期要还的一条**：170 回流 3（外部版本留存）。它不在冻结名单里——因为三选 UI 是本期范围，
而**没有留存就没有三选**：a 期有脏冲突是"先落盘"，外部那一版在磁盘上当场被覆盖。

---

### 2.2 范围（b 期做什么）

**徽章（W8）**
- [x] 直径 **≤8px 的小点**，悬停微放大；**不放数字、不放头像**——它是"这里有过一次对话"的
      存在感，不是通知角标
- [x] 位置：贴在锚点所在段落的**右缘**，随文档滚动与重排走
- [x] 点击 → 打开该线程的对话；徽章的 diff **从 git 取**（D-08），不存第二份正文

**落笔链接线（架构 §4.2 成对 commit）**
- [x] 落笔 → `commitPair('premarkup', 'markup')`：落笔**前**一次、**后**一次
- [x] **UI 先行 300ms，链在后台**：徽章与面板不等 git。落笔的手感不许因为 commit 变慢
- [x] **链失败徽章仍在，只是 diff 不可用**——对话是纸上真发生过的事，git 没记上不改变这一点

**线程面板（W11）**
- [x] 本篇线程列表 + **孤儿置灰区**（对不上锚点的线程沉到这里，不消失）
- [x] `threads/*.json` 落 `~/.sepia/books/<book-id>/`（与锚点同处，**不进 git**）

**⌘⇧H 还白（W10）**
- [x] 全隐 ↔ 全显来回切：一键把所有徽章收干净，再一键全回来

**锚点消费（a 期建的那套，本期第一次有人用）**
- [x] 打开文件时对齐一次
- [x] **6a 的外部变更事件触发重对齐**——两条并行线在这里真正汇合
- [x] 对不上就降级孤儿（**宁孤儿不误挂**，a 期已定的阈值一个不动）

**撤销联动（T-27）**
- [x] ⌘Z 撤掉落笔 → 徽章移出纸面、对话保留进置灰区；⌘⇧Z → 徽章回来
- [x] **不为它写专门的 undo 钩子**：落笔后锚点的引文就是改写后的文本，一撤引文就找不着，
      realign 自然判孤儿；重做则自然找回来。**锚点机制本身就是 T-27 的实现**——
      这是 a 期"宁孤儿不误挂"那条保守取舍的红利

**冲突三选完整版（170 回流 3）**
- [x] 覆盖前把外部那一版留一份到 `~/.sepia/books/<id>/conflicts/`
- [x] 有脏冲突从 a 期的"先落盘 + 横条"升级为**三选**：用我的 / 用外部的 / 都留着

**明确不做（刹车）**

| 会顺手带出的东西 | 为什么手会滑 | 刹车 |
|---|---|---|
| 文件树 / 多 Tab / 主页 | 冻结令刚解除，renderer 终于能动了 | **归 6b**，与本期串行 |
| 徽章上显示数字或头像 | "一眼看出有几条"很诱人 | W8 明文：小点。数字与头像是通知区的语言，不是纸的语言 |
| 线程面板做成侧边栏常驻 | 面板都做了 | 本期是**唤起式**；常驻布局归 6b 的整体 shell |
| 徽章 diff 自己存一份正文 | git 取 diff 要跑子进程，慢 | **D-08：diff 从 git 取**。存第二份正文就是第二个真相源 |
| 撤销联动写专门的 undo 钩子 | 直觉上"撤销"该有个 handler | 锚点已经能表达它。多一套钩子就多一处会漂的状态 |
| 给 Agent 开线程写权限 | 线程是对话，Agent 好像该能写 | **不变量 4**。线程由落笔链路写，Agent 够不到 |
| 三选做成模态对话框 | 三个选项，模态最省事 | 沿用横条形态升级，模态会打断书写（W11 的克制） |

---

### 2.3 结构与暴露面

| 包 | 新增 |
|---|---|
| `core` | `threads/`（线程类型 + 徽章/孤儿派生，纯函数）；冲突留存的命名规则 |
| `app` main | `books.ts` 扩线程与 conflicts 读写；`git.ts` 加 `diff()`；ipc 三项 |
| `app` renderer | 徽章层（CM6 装饰）、线程面板、⌘⇧H 命令、三选横条升级、锚点消费接线 |
| `editor` | 徽章装饰的宿主扩展（与 markup-host 同族） |

**桥：25 → 28（+3）**，全部申报在此：
`api.threads.load(dir)` / `api.threads.save(dir, threads)` / `api.git.diff(dir, before, after, page)`。
**没有 threads.delete**——删除是 save 一份不含它的表，少一个通道少一处不变量。
**依赖零新增**。**磁盘**：`~/.sepia/books/<id>/threads/*.json`、`conflicts/<时间戳>-<文件名>`。

---

### 2.4/2.5 harness 增量与反向验证

> 破坏方式预写，且**瞄"唯一那道保护"**（170 回流 6 的元规则）：
> 若某条性质有上下游两道保护，就把判据换到只有这一道能兑现的可观测量，
> 或明写"要两道全拆才红"。

| # | 检查 | 破坏方式（瞄唯一那道保护） |
|---|---|---|
| 1 | 单测 · 线程↔锚点派生：对得上 → 徽章；对不上 → 孤儿 | 让派生忽略 `orphan` → 孤儿线程冒出徽章必红 |
| 2 | 单测 · 线程往返（含特殊字符 page、空对话） | 存/读丢字段必红 |
| 3 | **单测 · 撤销联动**：落笔后引文变 → 撤销后 realign 判孤儿 → 重做后回来 | 把 realign 换成"永远返回上次位置" → 撤销后徽章不走，必红 |
| 4 | 单测 · 链失败不影响徽章 | 让 `commitPair` 抛错 → 断言徽章仍在、`diffAvailable=false`；**破坏方式是"失败时把徽章也删掉"** |
| 5 | 单测 · 三选矩阵（用我的/用外部的/都留着）+ **留存先于覆盖** | 让留存发生在覆盖之后 → 留下的是我们自己的版本，必红（**这是唯一那道**：覆盖之后就没有第二个地方能拿到外部版本） |
| 6 | smoke · 落笔 → 徽章出现（≤8px）→ 点开看得到对话 | 吞掉徽章装饰必红 |
| 7 | smoke · ⌘⇧H 全隐 ↔ 全显 | 只隐不显 / 只显不隐 必红 |
| 8 | **smoke · 徽章是"看得见"的**（像素级，160 §1.9 条目 7 的手法） | `width:0` / 去背景 / 被盖住 → 属性还在，必红 |
| 9 | smoke · 外部改文件 → 徽章重对齐或降级孤儿 | 不听 6a 事件 → 徽章停在旧位置必红 |
| 10 | smoke · 三选：选"用外部的"后正文是外部那一版，且我的那版在 conflicts/ 里 | 留存缺失必红 |

**反向验证结果（2026-08-06，五条预写破坏全部实证必红）**

| 破坏 | 结果 |
|---|---|
| 徽章装饰吞掉 spots | 红（#6） |
| 还白只隐不显 | 红（#7） |
| `realign` 恒返回原位（撤销联动的"唯一那道"） | 红（#3） |
| 留存改到覆盖之后 | 红（#10） |
| 不听外部变更（`refreshNow` 空转） | 红（#9） |

**两条测试自身的教训**（都是"测的不是要测的那件事"，002 §6.2 同科）：

1. **撤销那条一开始在跟落笔链赛跑**。落笔会触发写盘、写盘惊动 watcher，
   watcher 那条路可能在撤销**之后**才把盘上的内容重载回来——于是"徽章又回来了"，
   而那与撤销毫无关系。两次跑出两种结果（一次绿一次红）就是信号。
   修法是**先等链落停再撤销**，不是加个 sleep 了事。
2. **三选那条测的根本不是有脏冲突**。harness 里自动写盘是 300ms，
   外部改动之前字早就存进去了 → `decideExternalChange` 判的是无脏重载，
   静默走掉，**一条横条都不会出**。修法是把该用例的防抖调到 60s，
   让"脏"这个前提真的成立。

---

### 2.6 验证（两栏）

**CC 代验**：上表 10 条 + RV 记录 + `check` PASS + 全量 smoke。

**真人（压到最少）——机器证不了的三件事，明确留给人**：
- [ ] **徽章观感与克制度**：8px 的点在真实长文里是"陪着"还是"打扰"；悬停微放大的分寸
      （机器能证尺寸与可见，证不了"克制"）
- [ ] **还白瞬间的体感**：⌘⇧H 一按，满纸徽章消失的那一下——是干净利落还是突兀
- [ ] **三选的措辞**：三个选项的名字能不能让人在三秒内选对（选错的代价是自己的字）
- [ ] 抽证据包 2 条

---

### 2.7 实测

**按人裁冻结口径：本期不做性能测量**，数字随 MVP 后的 release 专项一并补。

只记一个功能性事实：**落笔到徽章出现不等 git**——线程在落笔那一刻就建好并画出徽章，
`commits` 留空，链回来了再补。smoke #6 断言的正是这条行为（徽章出现时链尚未回来）。

---

### 2.8 风险

| # | 风险 | 探法 |
|---|---|---|
| 1 | 徽章装饰与 markup 浮层同为块级 widget，可能抢位 | 先探：徽章走**行内 widget 贴右缘**，与浮层的块级 host 不同族 |
| 2 | 重对齐的时机太密（每次 doc change 都算） | 防抖 + 只对当前 page 的锚点算；长文实测 |
| 3 | git diff 取不到（浅克隆 / commit 被 gc） | 降级为"diff 不可用"，徽章与对话都不受影响（与链失败同一条路） |
| 4 | 撤销联动依赖 realign，undo 后引文若恰好仍能匹配则徽章不走 | 用例覆盖"改写与原文高度相似"的边角；必要时把落笔后的锚点引文钉死为**改写后原文**而非重新取材 |

### 2.9 回流

| # | 指向 | 问题 / 偏离 | 处置 |
|---|---|---|---|
| 1 | **160 §2.3 的桥申报漏了成对提交的触发口** | 写计划时申报了 `threads.load/save` + `git.diff` 三项，**漏想了"谁来触发 commitPair"**。补的时候发现：从 renderer 发三次调用（提交前 → 写 → 提交后）中间会插进自动写盘的防抖，**夹出来的两点就不是这一次落笔的** | 不加第 29 项，改成既有写通道的一个**选项**：`file.write(path, content, { markupPair })`。桥仍是申报的 28 项，而且顺序由 main 保证——比多开一个 key 更对 |
| 2 | **"用外部的"一度想给 `MountedEditor` 加"整篇替换"** | 三选要把外部那版灌进编辑器，直觉是加一个 `replaceAll` | **没加**。那会在类型上开出第二条写正文的路，而不变量 3 的机器保证正是靠"拿不到 `EditorView` 就绕不过 CAS"。改走两条既有路径：写盘（⌘S 那条）+ 重载（打开文件那条） |
| 3 | **同一个坑的第三次：绝对路径 → 相对 repo 根要两侧都 realpath** | `git diff` 因 `/var` vs `/private/var` 报 "outside repository"，**diff 恒返回 null、徽章点开永远是空的**。此前 trailer（5a）、book-id（5a）各栽过一次 | 收进一个 `toRepoRelative` 函数。**建议升级为纪律**：凡"绝对路径要相对某个根"的地方一律两侧 realpath——三次同因，已经不是巧合 |
| 4 | **测试自身的两处"测的不是要测的那件事"** | ① 撤销那条跟落笔链赛跑（写盘→watcher→重载可能后于撤销落地）；② 三选那条根本没造出"有脏"（300ms 防抖早把字存了） | 均已按 002 §6.2 的元规则处置：先修检查再重跑破坏，明细进 §2.4/2.5 |

---

## Stage 5a 完成（2026-08-06）

**dod_a 四条判定：**

| DoD | 判定 | 据什么 |
|---|---|---|
| 写字 → 停 800ms 自动落盘 | **达成** | smoke #7（没按 ⌘S，防抖到点即落盘）；阈值进 config |
| 静默/定时 commit 产生且个位数、带 trailer | **达成** | smoke #7 断言 `sepia: save` + `Sepia-Page: note.md`（相对路径）+ commit 数 < 10；#7b 断言无改动不再提交 |
| 锚点四级标定用例全过 | **达成** | 15 条单测，真实长文标定集，四级各 2 例 + **三条误挂反证** |
| 并发 commit 不撞 index.lock | **达成** | 单测 #1 在真 repo 里 8 发并发；去掉队列实测 7 发失败并报出 index.lock 原文 |

**a 期没做、且是有意不做的**：徽章/线程/⌘⇧H/成对 commit 接线（b 期）、watcher（L3）、
锚点的消费者（本期只有读写与对齐，没人用它）。**性能不测**（继承 150 的人裁口径）。

**暴露面**：桥**零增长**（仍 20 项，§1.9 条目 5 撤掉了原计划的 `onWriteStatus`）；
依赖**零新增**（系统 git CLI）；config 增四项（`autosaveDebounceMs` / `commitIdleMs` /
`commitIntervalMs` / `anchorFuzzyThreshold`），全部真被读取；
磁盘增 `~/.sepia/books/<book-id>/{meta,anchors}.json`。

**给 L3 的话**：共享接缝 `createSelfWriteLog()` 已在 `@sepia/core` 里，
`main/ipc` 暴露 `savePipeline()?.selfWrites` 供 claim。**写盘管线已在 record 那一侧接好**——
L3 rebase 之后直接消费即可，不必再用本地桩。

### 遗留债（b 期或 release 专项）

> **人裁 2026-08-06：全部技术债冻结至 MVP（Stage 7 关闭）后统一处理。**
>
> 冻结的范围是**两个 a 期的合并清单**——release 专项三债（markup 性能实测 / 冷启动基线重立 /
> `.dmg` 打包重验，含双击 .md 与游离打开的人工走查）、6a 债 A（`files.trash` 废纸篓可见性）、
> 锚点标定集代表性、`commitPair` 接线、可见性断言升像素级、架构 §4.9 监听有界、
> 外部版本留存。**后续 plan 的 §1.1 对这些债只列不裁**——列是为了不失忆，不裁是因为
> 裁决时机已经由人定死在 Stage 7 之后。
>
> **唯一例外：typecheck 增量化保留触发式。** 它挡的是开发速度本身，红线（`check` >20s
> 或 typecheck 冷跑 >10s）到了还是得做，不等 MVP。


| # | 债 | 何时还 |
|---|---|---|
| ~~1~~ | ~~**§1.6 真人三条**（落盘无感、`git log` 观感、抽证据包）~~ | **✓ 已还**（2026-08-06 真人轮全部通过，并抓出警示点缺陷一枚） |
| 2 | markup 性能实测 + 冷启动基线重立 + `.dmg` 重验（继承 150 债 1/2/3） | **建议 a 期合并后插一次 release 专项一并清**（三者互相咬着） |
| 3 | typecheck 增量化（触发式） | 未触发。本期 `check` 冷跑约 20s，已接近 §1.1 二写死的阈值，下期先量一次 |
| 4 | 锚点标定集的代表性 | 本期用仓库自己的文档标定（§1.9 条目 3）；b 期真实使用后回标，必要时人裁指定外部语料 |
| 5 | `commitPair` 未接线 | b 期接落笔链（落笔前 `pre-markup`、落笔后 `markup`） |

---

## Stage 5b 实施完成（2026-08-06）——**人工轮等人**

**范围六项全部落地**：徽章（W8）、落笔链接线成对 commit、线程面板 + 孤儿置灰区（W11）、
⌘⇧H 还白（W10）、锚点消费（打开 + 外部变更重对齐）、撤销联动（T-27）、
冲突三选完整版（170 回流 3 一并还掉）。

**机器侧**：`check` PASS（豁免仍 4 / dispute 0）；smoke **41/41**（本期新增 6 条）；
五条预写破坏全部实证必红。

**暴露面**：桥 25 → **28**，与 §2.3 的申报一致（`threads.load/save` + `git.diff`）。
成对提交的触发**没有占用第 29 项**——它是既有写通道的一个选项（§2.9 条目 1）。
依赖零新增。磁盘增 `threads/*.json` 与 `conflicts/`。

**shell 冻结令**：本分支已解除，改动按最小面记账——App.tsx 增线程仓生命周期、
onChange 一行重算、两条命令、面板与三选的渲染；`EditorHost` 未动；
`MountedEditor` 加了 `showBadges` / `toggleBadges` 两个方法，**仍不交出 `EditorView`**。

### 留给人工轮的三件事（机器证不了）

| # | 项 | 为什么机器证不了 |
|---|---|---|
| 1 | **徽章观感与克制度** | 机器能证 ≤8px、能证像素可见、能证里面没有数字没有头像；证不了它在真实长文里是"陪着"还是"打扰"，也证不了悬停微放大的分寸 |
| 2 | **还白瞬间的体感** | ⌘⇧H 一按满纸徽章消失的那一下，是干净利落还是突兀——只有人能判 |
| 3 | **三选的措辞** | 三个名字能不能让人在三秒内选对。**选错的代价是自己的字**，所以这一条比前两条都重 |

另：`threads.panel` 命令**尚未绑键**（⌘/ 命令面板归 Stage 7、侧栏归 6b），
现在只能经命令注册表调起——与 6a 债 A（`files.trash` 无入口）同科，一并等 6b。
