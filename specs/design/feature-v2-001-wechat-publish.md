# FEATURE-v2-001 · 公众号 Preview / 导出复制

> **状态：v2 第一优先，明确要做**（用户 2026-08-02 定，D-24），不进 MVP。
> 定位：把现有终端管线（`pinzai-wechat-publish` skill + `publish-prep.sh` / `preview.sh`）产品化进 Sepia，终态是"我要做的只剩点一下发布"。

## 功能拆解（用户原始描述的五件事）

| # | 子功能 | 现状 | v2 要做的 |
|---|---|---|---|
| 1 | **图片上传 OSS** | `publish-prep.sh` 已实现（bucket `blogimgbeg`，`YYYY/MM/DD/1mg/`，public-read，改写 md 引用，输出 `.oss.md` 不覆盖原文件） | 内建化：Sepia 内一键执行，复用既有路径约定与「不覆盖原文件」纪律 |
| 2 | **shader → GIF** | 无（新能力） | `/shader` 块离屏渲染 N 秒 → 编码 GIF/APNG → 当作普通图片走 #1 上传，md 中替换为图片引用。参数：时长/帧率/分辨率有默认值，不出设置项 |
| 3 | **公众号 md 格式转换** | doocs/md 手工粘贴（已验证流程） | 内嵌 doocs/md 的转换核（或调用本地 `~/w/art/md`），主题固定一套，输出公众号编辑器可粘贴的富文本 |
| 4 | **底部链接等固定件** | 手工 | 模板化：底部往期链接 / 引导语等，book 级模板文件（人手改 → 按 D-22 放 `.sepia/publish-template.md`） |
| 5 | **自动存为公众号草稿** | 无（新能力） | 调公众号「新建草稿」API（draft_add），封面用 frontmatter `cover`。做完后用户只需在公众号后台点发布 |

## 交互设想

- 入口克制：不是常驻按钮。命令形式待定（候选：文件树右键「发布预览」/ `⌘⇧P`），**不进正文 UI**
- 产物遵守 A2：生成 `.oss.md` / 富文本剪贴板，**永不改写原 .md**
- 全过程一屏预览：转换结果 + 图片上传清单 + 草稿投递状态

## 前置依赖与开放问题

- [ ] 公众号草稿 API 需要认证服务号/订阅号 + IP 白名单 + appid/secret 保管（放应用级 `config.json`？还是系统钥匙串？）
- [ ] shader 离屏渲染在 Electron 主/渲染进程的归属；GIF 编码库选型（gifski？）
- [ ] doocs/md 转换核的引用方式：npm 依赖 vs 调用本地 clone（`~/w/art/md`）
- [ ] 模板文件的 frontmatter 约定（cover / 摘要 / 底部链接列表）
- [ ] 失败降级：API 投递失败时退回「复制到剪贴板 + 打开公众号后台」的手动路径（即现有流程）

## 关联

- 取代 non-goals 表 2 中「导出/发布按钮」条目的永久否决——**v1 仍不做**，v1 走现有终端管线（happy path 分镜 14）
- 与 D-22 格式原则、A2 字节承诺一致；与 pinzai-publish（标题/摘要/标签工作流）衔接可后议
